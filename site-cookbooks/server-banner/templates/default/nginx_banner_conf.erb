server {
  listen 80 default_server;
  listen [::]:80 default_server ipv6only=on;
  listen 443 default_server ssl;
  listen [::]:443 default_server ipv6only=on ssl;

  server_name <%= @name %>;
  location / {
    root   /var/www/server-banner;
    index  index.html index.htm;
  }
}
